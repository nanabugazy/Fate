.container
  .row
    .col-md-6
      h1.form-signin-heading Your profile
      .form-horizontal
        .form-group
          label.col-sm-3.control-label Full name
          p.form-control-static.
            {{ details?.name }}
        .form-group
          label.col-sm-3.control-label Email
          p.form-control-static {{ details?.email }}
