.container
  .row
    .col-md-6
      h1.form-signin-heading Sign in
      p.lead
        | Not a member? Please 
        a(routerLink='/register') register
        |  instead.
      form((submit)='login()')
        .form-group
          label(for='email') Email address
          input.form-control(type='email', name='email', placeholder='Enter email', [(ngModel)]='credentials.email')
        .form-group
          label(for='password') Password
          input.form-control(type='password', name='password', placeholder='Password', [(ngModel)]='credentials.password')
        button.btn.btn-default(type='submit') Sign in!
